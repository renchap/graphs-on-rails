%h2= 'Repository: ' + @repository.name
%ul.details
  %li= "Type: " + @repository.type
  %li
    Options:
    %ul
      - @repository.options.each do |name, value|
        %li= "#{name.capitalize}: #{value}"